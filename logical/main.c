#include <stdio.h>

int main() {

    // its basicly js or any other prog lang
    // Im watching 6h of this guy explaining basic things I know
    // tho I saw some things that C does different
    // so imma watch the entire thing ngl

    int temp = 20;

    if (temp > 0 && temp < 30) printf("The temp is GOOD!!");
    else printf("temp is bad");

    return 0;
}