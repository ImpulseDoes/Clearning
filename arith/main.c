#include <stdio.h>

int main() {

    int x = 10;
    int y = 2;
    int z = 0;

    // z = x + y;
    // z = x * y;
    // z = x / y;
    // z = x - y;

    // printf("%f", z);

    // int / int = int 0
    // int / int = float 0.0000
    // int / flaot = float 0.6767677

    // for devision we need to use floats

    // modulo

    // z = x % y;

    // printf("%d", z);

    // increment & decremept

    // x++;

    // printf("%d\n", x);

    // x--;

    // printf("%d\n", x);

    // and this is in other langs
    int c = 10;

    // c += 2;
    // c -= 2;
    // c *= 2;
    // c /= 2;

    printf("%d", c);

    return 0;
}